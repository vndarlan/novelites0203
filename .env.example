# Variáveis de ambiente para a aplicação
# Renomeie este arquivo para .env para desenvolvimento local

# Variável de ambiente do Railway - não alterar em produção
# DATABASE_URL será fornecido pelo Railway automaticamente

# Para desenvolvimento local, você pode usar SQLite (já configurado por padrão)
# ou configurar um PostgreSQL local descomentando a linha abaixo:
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/site_agente

# Porta para o servidor web
PORT=8501

# Configurações do aplicativo
DEBUG=False